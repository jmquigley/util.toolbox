language: node_js
dist: trusty
cache: yarn
sudo: required

node_js:
    - '7'

before_install:
    - 'curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.0.0'
    - export PATH="$HOME/.yarn/bin:$PATH"

before_script:
    - 'npm run build'

after_success:
    - 'npm run coverage'
